# P1 validation record

Date: 2026-09-08. Phase: local reference implementation, not distributed integration.

Environment: macOS arm64, Java 21.0.8, Maven 3.9.16 (wrapper), Spring Boot 4.1.1, Node.js 23.9.0.

Commands:
```sh
./mvnw --batch-mode --no-transfer-progress verify
make demo
```

Result: 27 tests, 0 failures, 0 errors, 0 skipped. Packaged HTTP demo assertions passed; child JVM terminated.
ReservationsTest: 20 tests. ApiTest: 6 tests. ApplicationTest: 1 health test.

Coverage includes conservation, replay/payload conflicts, expiry equality, repeated transitions, tenant keys, bounded history, 1,000 competing tasks for 100 units (16 executor threads), duplicate requests, confirm/cancel races and a seeded 1,000-operation sequence.

HTTP tests cover request/response serialization, ProblemDetail errors and rejection of malformed, missing, null, fractional and string-coerced numeric fields.

Limitations: Docker daemon unavailable. No PostgreSQL, Kafka, OpenSearch, multi-instance, container build, load/soak, security audit, backup/restore or cloud deployment validation. Test duration is not a throughput benchmark. CI runs Maven verification and the packaged demo; inspect the exact main commit's run.

Additional packaged-runtime check: starting without the local-demo profile exits with status 1 and the expected deployment guard message.

Known nonfailing test warning: Spring's test support loads Mockito's agent dynamically under Java 21. No mocks are used in business tests; future JDK/tooling upgrades should revisit explicit agent configuration.
